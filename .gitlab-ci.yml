image: docker:stable

stages:
  - build
  - test

services:
  - java:8

build:
  stage: build
  tags:
  - shell
  script:
  - ./build.sh

test:
  stage: test
  tags:
    - shell
  script:
  - ./test_e2e.sh
